First script
using Unity.VisualScripting;
using UnityEngine;

public class red : MonoBehaviour
{
    int variable = 0;
    
    // Start is called before the first frame update
    void Start()
    {
        variable = 5;
        Debug.Log("interger variable = " + variable);
    }

    // Update is called once per frame
    void Update()
